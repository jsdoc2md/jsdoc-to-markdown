{{#each (where this kind="module")}}
* {{name}}
  {{#each (where .. kind="member" memberof=longname)}}* [{{name}}](#{{{longname}}})
  {{/each}}{{#each (where .. kind="function" memberof=longname)}}* {{name}}({{methodSig params}})
  {{/each}}{{#each (where .. kind="class" scope="inner" memberof=longname)}}* Class: {{name}}
  {{/each}}
{{/each}}
