{{! Module}}
{{#findWhere (where this kind="module")}}#{{name}} API
{{{description}}}

{{! Module properties}}
{{#each (where .. kind="member" memberof=(concat "module:" name))}}
{{>properties parentName=../name}}
{{/each}}

{{! Module Methods}}
{{#each (where .. kind="function" memberof=(concat "module:" name))}}
{{>function parentName=../name}}
{{/each}}

{{! Module Classes}}
{{#each (where .. kind="class" memberof=(concat "module:" ../name))}}
{{>class}}

{{! Module Class properties}}
{{#each (where ../.. kind="member" memberof=(concat "module:" ../name "~" name))}}
{{>properties parentName=../name}}
{{/each}}

{{! Module Class methods}}
{{#each (where ../.. kind="function" memberof=(concat "module:" ../name "~" name))}}
{{>function parentName=../name}}
{{/each}}

{{! Module Class events}}
{{#each (where ../.. kind="event" memberof=(concat "module:" ../name "~" name))}}
{{>event}}
{{/each}}

{{/each}}
{{/findWhere}}

{{! Classes}}
{{#each (where this kind="class")}}
{{>class}}

{{! Module Class properties}}
{{#each (where ../.. kind="member" memberof=(concat "module:" ../name "~" name))}}
{{>properties parentName=../name}}
{{/each}}

{{! Module Class methods}}
{{#each (where ../.. kind="function" memberof=(concat "module:" ../name "~" name))}}
{{>function parentName=../name}}
{{/each}}

{{! Module Class events}}
{{#each (where ../.. kind="event" memberof=(concat "module:" ../name "~" name))}}
{{>event}}
{{/each}}
