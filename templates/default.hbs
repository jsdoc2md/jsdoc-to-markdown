{{! Module}}
{{#findWhere (where this kind="module")}}#{{name}} API
{{{description}}}

{{! Module properties}}
{{#each (where .. kind="member" memberof=(concat "module:" name))}}
{{>properties parentName=../name}}
{{/each}}

{{! Module Methods}}
{{#each (where .. kind="function" memberof=(concat "module:" name))}}
{{>function parentName=../name}}
{{/each}}

{{! Classes}}
{{#each (where .. kind="class")}}
{{>class}}
{{#each (where ../.. kind="member" memberof=(concat "module:" ../name "~" name))}}
{{>properties parentName=../name}}
{{/each}}

{{!>methods of=name namePrefix=name jsdocdata=../..}}
{{!>events of=name jsdocdata=../..}}
{{/each}}

{{/findWhere}}